# Environment Configuration for Cipher Talk Application

# MongoDB Connection String
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database

# Server Port (default: 3000)
PORT=3000

# Node Environment (development/production)
NODE_ENV=development

# Production Domain (required for hosting)
# Replace with your actual domain when deploying
PRODUCTION_DOMAIN=yourdomain.com

# Note: Update PRODUCTION_DOMAIN with your name.com domain before hosting
# Example: PRODUCTION_DOMAIN=myapp.name.com
